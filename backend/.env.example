# Copy this file to .env and fill in your local MySQL credentials

# Server
PORT=4000
NODE_ENV=development

# MySQL connection
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=project_tracker
DB_USER=root
DB_PASSWORD=vikash@12

# Used only when running `npm test`
TEST_DB_NAME=project_tracker_test
